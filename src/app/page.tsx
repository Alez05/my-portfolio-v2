import { Button, FlexBox, GridBox, Icon, Input, NavBar, Text } from '@/libs/ui';

const Home = () => {
  return (
    <main>
      <NavBar />
      <h1>Hello world</h1>
    </main>
  );
};

export default Home;

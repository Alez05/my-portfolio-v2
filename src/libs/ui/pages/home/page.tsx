import { Layout, Maintenance } from '@/libs/ui';
import { HomeHeader } from '../../organism';

const HomePage = () => {
  return (
    <Layout>
      <HomeHeader />
    </Layout>
  );
};

export default HomePage;
